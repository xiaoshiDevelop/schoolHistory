<!--pages/birthplace_sub/birthplace_sub2.wxml-->
<view class="container">
  <block wx:for="{{interview}}" wx:key="key">
    <view class="introduce">
      <view class="question">{{item.number}}：{{item.question}}</view>
      <view class="answer">{{item.answer}}</view>
    </view>
  </block>
</view>